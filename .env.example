# Echo Environment Variables

# MongoDB connection URI (required in production)
ECHO_MONGO_URI=mongodb://localhost:27017/echo

# Comma-separated list of allowed CORS origins (required in production)
ECHO_CORS_ORIGINS=http://localhost:5173

# Salt for IP hashing - use a secure random string in production (required)
ECHO_IP_SALT=your-secure-random-salt-here

# Rate limit: max POST requests per IP per minute (optional, default: 5)
ECHO_RATE_LIMIT_PER_MINUTE=5

# Spring profile (dev/prod)
SPRING_PROFILES_ACTIVE=dev

# Frontend API URL
VITE_API_BASE_URL=http://localhost:8080
